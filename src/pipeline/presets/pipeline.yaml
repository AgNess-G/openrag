version: "1"
ingestion_mode: composable

parser:
  type: auto

chunker:
  type: recursive
  chunk_size: 1000
  chunk_overlap: 200

embedder:
  provider: openai
  model: text-embedding-3-small

execution:
  backend: ray
  concurrency: 4
